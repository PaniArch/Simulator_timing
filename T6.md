先读取并遵守现有 `docs/architecture.md`，在 T5 已完成的 Multi-Warp Core 基础上完成 **T6：CTA + Barrier**。

本阶段目标是加入 CTA 层，使同一 CTA 内的多个 Warp 能够共享必要的 CTA context、local/shared memory，并通过 barrier 正确同步和恢复执行。

主要完成：

1. 建立 CTA 与 Warp 的归属关系，明确一个 CTA 包含哪些 Warp，并维护 CTA ID、成员关系和完成状态。
    
2. 建立 CTA 级 context，使相关 thread/warp 能够获得当前冻结 RTL 所需的 CTA/thread 信息，例如 CTA ID、thread context、block/grid 相关只读信息等。具体内容以 Architecture Contract 和 RTL 为准。
    
3. 接入 local/shared memory，使同一 CTA 内的 Warp 和 lanes 可以访问同一份对应的 local memory 区域，并保持与 global memory 清晰区分。
    
4. 将 T1 已完成的 barrier 指令语义接入真实 CTA 执行，包括：
    
    - BAR；
        
    - BAR.ARRIVE；
        
    - BAR.WAIT。
        
5. 建立 barrier 所需的功能状态，例如 participant、arrival、wait、phase 等当前 RTL 实际需要的状态，并保证这些状态只有一个 canonical owner。
    
6. 当 Warp 执行到需要等待的 barrier 时，使其进入 blocked 状态；当 barrier 条件满足后，正确恢复对应 Warp，使其重新进入 runnable 状态继续执行。
    
7. 将 barrier 行为接入 T5 已有的 Multi-Warp Core 和 scheduler，使 scheduler 不选择仍处于 barrier wait 状态的 Warp，但能够在 barrier 解除后继续推进这些 Warp。
    
8. 建立 CTA completion：当该 CTA 内所有相关 Warp 都正确完成，并且不存在仍需处理的 CTA/barrier 状态时，CTA 可以报告完成。
    
9. 扩展测试和 trace，验证：
    
    - Warp 与 CTA membership；
        
    - CTA/thread context；
        
    - local/shared memory 在同一 CTA 内正确共享；
        
    - 不同 CTA 的 local memory/CTA state 不发生错误混用；
        
    - BAR；
        
    - BAR.ARRIVE；
        
    - BAR.WAIT；
        
    - barrier participant/phase；
        
    - Warp block 和 resume；
        
    - 多 Warp 在 barrier 前后继续正确执行；
        
    - CTA completion。
        

本阶段不实现：

- 完整 Kernel grid launch；
    
- 多 CTA 的 Kernel 调度；
    
- Kernel 参数/入口初始化的完整流程；
    
- Kernel completion；
    
- 多 Core；
    
- cycle-accurate barrier timing；
    
- cache/MMU/pipeline/性能模型。
    

当前只要求 CTA 内的功能协同正确，不需要复刻 RTL barrier unit 的周期、队列或握手行为。

整个过程中持续维护 `docs/architecture.md`。本阶段重点验证 CTA ownership、Warp membership、thread/CTA context、local memory ownership、barrier state、blocked Warp 恢复以及 CTA completion 等此前的 `PROVISIONAL` / `UNRESOLVED` 内容。实际实现和 RTL 研究发现问题时必须同步更新 Contract。

完成后运行项目现有完整验证，并保持 T1–T5 原有测试通过。

**T6 完成标准：**

- CTA 与其成员 Warp 的关系明确且可以正确维护；
    
- CTA/thread context 能够提供后续执行需要的功能信息；
    
- 同一 CTA 内能够正确共享 local/shared memory；
    
- BAR、BAR.ARRIVE、BAR.WAIT 能够在真实 Multi-Warp 环境中工作；
    
- barrier participant、arrival、wait、phase 等功能状态正确；
    
- 等待 barrier 的 Warp 会被正确 blocked；
    
- barrier 满足后相关 Warp 能正确恢复并继续执行；
    
- scheduler 与 barrier 状态能够正确协同；
    
- 一个 CTA 内多个 Warp 可以通过 local memory 和 barrier 完成协作执行；
    
- CTA completion 能够正确判断；
    
- Architecture Contract 与实际实现保持一致；
    
- 项目完整验证通过。
    

T6 最终交付的是 **一个能够让同一 CTA 内多个 Vortex Warp 通过共享 local memory 和 barrier 正确协同执行的功能模型**。下一阶段 T7 再加入 Kernel 输入、CTA 创建和完整 Kernel execution。