先读取并遵守现有 `docs/architecture.md`，在 T4 已完成的 SIMT Warp 基础上完成 **T5：Multi-Warp Core**。

本阶段目标是把单 Warp 执行扩展到冻结配置下一个 Core 内的多个 Warp，使这些 Warp 能够被正确创建、激活、选择执行、等待和结束。

主要完成：

1. 根据冻结配置建立一个 Core 内实际需要的 Warp slots，并直接复用 T4 已完成的 WarpState 和 Warp execution，不复制或重写 Warp 内部状态和语义。
    
2. 建立 Core 级 Warp 管理状态，能够区分哪些 Warp：
    
    - inactive；
        
    - runnable；
        
    - blocked；
        
    - finished。
        
3. 建立简单、确定性的功能级 Warp scheduler，在 runnable Warp 中选择一个继续执行。可以采用 round-robin 等简单策略，只保证功能执行正确和各 Warp 能持续获得执行机会。
    
4. 将 T1 已实现的 WSPAWN 语义接入真实 Multi-Warp Core，根据冻结 RTL 完成目标 Warp 的激活和必要初始化，使新 Warp 可以进入正常执行。
    
5. 将 WSYNC 接入真实 Multi-Warp 执行，根据 RTL 和 Architecture Contract 实现其功能级等待/恢复行为。
    
6. 建立 Core 级连续执行，使 scheduler 可以反复选择 Warp、调用已有 Warp execution，并根据执行结果更新 Warp lifecycle。
    
7. 正确处理 Warp completion：某个 Warp 完成后不再参与调度，但其它 Warp 可以继续执行；当所有相关 Warp 都完成时，Core 可以报告完成。
    
8. 扩展测试和 trace，验证多个 Warp 的 State 相互隔离，scheduler、WSPAWN、WSYNC、blocked/runnable/finished 状态和 Core completion 均正确。
    

本阶段不实现：

- CTA 的完整状态和调度；
    
- BAR/BAR.ARRIVE/BAR.WAIT 的真实 barrier completion；
    
- Kernel grid/CTA launch；
    
- Kernel completion；
    
- RTL 的周期级 scheduler；
    
- scoreboard、pipeline、issue timing；
    
- cache/MMU 或性能模型。
    

BAR 等需要 CTA 协同的行为继续保留到 T6，不要在 WarpManager 中提前实现一套临时 barrier。

整个过程中持续维护 `docs/architecture.md`。本阶段重点根据真实 Multi-Warp 实现验证 Warp lifecycle、active Warp ownership、WSPAWN、WSYNC、scheduler/Core 与 Warp 的职责边界；发现问题时同步更新 Contract，不保留与实际 RTL/实现不一致的旧设计。

完成后运行项目现有完整验证，并保持 T1–T4 原有测试通过。

**T5 完成标准：**

- 一个 Core 能够持有冻结配置要求的多个 Warp；
    
- 每个 Warp 的 PC、register、mask、divergence 等 State 相互独立；
    
- Warp 能正确处于 inactive/runnable/blocked/finished 状态；
    
- 简单功能 scheduler 能正确推进多个 runnable Warp；
    
- WSPAWN 能正确激活并初始化新的 Warp；
    
- WSYNC 能在 Multi-Warp 环境中正确工作；
    
- Warp block、resume 和 completion 行为正确；
    
- 一个 Warp 完成不会影响其它 Warp 继续执行；
    
- 所有 Warp 完成后 Core 能正确结束；
    
- Architecture Contract 与实际实现保持一致；
    
- 项目完整验证通过。
    

T5 最终交付的是 **一个能够在单 Core 内正确管理和连续执行多个 Vortex Warp 的功能模型**。