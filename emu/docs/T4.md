先读取并遵守现有 `docs/architecture.md`，在 T1 ISA、T2 Architectural State 和 T3 Single-Lane Warp Executor 基础上完成 **T4：SIMT Warp**。

本阶段目标是把 T3 的单 active lane Warp 扩展为冻结配置下的完整 SIMT Warp，使一个 Warp 内的 4 个 lanes 可以按照 Vortex 的功能语义连续正确执行。

主要完成：

1. 将现有 Warp 扩展到冻结配置要求的 4 lanes，复用已有 `LaneState`、`WarpState` 和 `Warp.Step()`，不要重新建立第二套执行模型。
    
2. 接入真实 active mask，使每条普通指令只对当前 active lanes 生效，inactive lanes 不产生架构写回。
    
3. 扩展普通 ISA 指令的执行，使 integer、FP、load/store、branch 等已有语义能够在多个 active lanes 上正确执行，并将结果提交到对应 Lane/Warp State。
    
4. 完成冻结配置中 Warp 内部 SIMT/custom 指令的真实执行，包括当前 RTL 实际启用的：
    
    - TMC / PRED；
        
    - SPLIT / JOIN；
        
    - VOTE；
        
    - SHFL；
        
    - WGATHER；
        
    - 其它属于单 Warp 范围的 cross-lane 指令。
        
5. 完成 Warp 内 divergence / reconvergence，使 SPLIT/JOIN 可以在连续执行过程中正确改变 active mask、执行路径和相关 Warp State。
    
6. 保证 cross-lane 指令能够读取当前 Warp 所需的 lane 数据，并将结果正确写回对应 lanes。
    
7. 扩展连续执行和 trace/test，使包含普通指令、mask control、divergence 和 cross-lane 指令的简单程序能够由一个 4-lane Warp 连续运行并得到正确结果。
    

本阶段不实现：

- 多 Warp 调度；
    
- WSPAWN 的真实多 Warp 创建；
    
- WSYNC 的 Core 级协调；
    
- BAR/BAR.ARRIVE/BAR.WAIT 的真实跨 Warp barrier；
    
- CTA；
    
- Kernel execution；
    
- 周期、流水线、cache 或性能模型。
    

WSPAWN、WSYNC、BAR 等超出单 Warp 范围的指令继续保持已有 effect/control 边界，留给后续阶段处理，不在 T4 中伪造其它 Warp 或 CTA 状态。

整个过程中持续维护 `docs/architecture.md`。如果真实 4-lane 执行验证或推翻了此前关于 active mask、instruction scope、divergence、cross-lane effect、Warp PC 或 State ownership 的设计，应同步更新 Contract；新问题记录为 `UNRESOLVED`，已解决问题更新状态。

完成后运行项目现有完整验证，并保持 T1–T3 原有测试通过。

**T4 完成标准：**

- 一个 Warp 具有冻结配置要求的 4 个 lanes；
    
- active mask 能正确控制 lane 执行；
    
- 普通 ISA 指令能够在所有 active lanes 上正确执行；
    
- TMC/PRED 能正确改变 Warp mask；
    
- SPLIT/JOIN 能完成 divergence 和 reconvergence；
    
- VOTE/SHFL/WGATHER 等 Warp 内 cross-lane 指令能够正确执行；
    
- Warp PC、mask、lane State 和 divergence State 在连续执行过程中保持正确；
    
- 一个完整 4-lane Warp 可以连续执行简单 SIMT instruction stream；
    
- Architecture Contract 与实际实现保持一致；
    
- 项目完整验证通过。
    

T4 最终交付的是 **一个能够连续正确执行的单个 4-lane Vortex SIMT Warp**。